build/
build_counter.properties
.gradle/
.caches/
remapped_mods/
*.lock
*.class
.DS_Store
archived/
push-website.sh
release.sh
AGENTS.md
# Custom mods copied into game/mods by the launcher at each launch —
# local-only, never commit the jars (Essential is ~53MB).
mods/
